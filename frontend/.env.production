# Production build config (committed — these AdSense IDs are PUBLIC, not secrets).
# `.env` is gitignored, so this file is what a fresh `vite build` on the server actually reads.
# Replace the placeholder slot IDs below with the real ad-unit IDs from your AdSense console.
VITE_ADSENSE_CLIENT=ca-pub-9972634875622184
VITE_AD_SLOT_LEADERBOARD=1234567890
VITE_AD_SLOT_RECTANGLE=2345678901
VITE_AD_SLOT_IN_ARTICLE=3456789012
